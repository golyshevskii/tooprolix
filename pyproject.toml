[build-system]
requires = ["maturin>=1.14,<2.0"]
build-backend = "maturin"

[project]
name = "tooprolix"
# NOT a literal `version = "..."`: the single source of truth is `[package] version` in Cargo.toml,
# which is the file release-plz bumps from the Conventional Commits. Writing the number in two
# places is how they drift, and hand-editing either one is exactly what
# `package-python-distribution-and-publish-0-1-0` AC5 forbids.
dynamic = ["version"]
description = "A prose budget linter for Python."
# The PyPI project page. It is NOT this file verbatim: `scripts/transform_readme.py` rewrites
# EVERY relative address README.md carries into an absolute GitHub URL first, because a relative
# address has no repository to resolve against on a project page and 404s. No count is written
# down: `TestTheRealReadmeIsFullyResolved` pins that ZERO survive, which stays true as the README
# grows links, and an exact number here would not. That script runs as a
# step in `.github/workflows/build-artifacts.yml` before maturin reads this key, and it FAILS the
# build rather than warning when the README is not the shape it expects.
readme = "README.md"
keywords = ["comments", "docstrings", "linter", "prose", "python"]
# PEP 639: an SPDX expression plus the file, not the retired `License ::` trove classifiers —
# `license-files` puts both required `License-File` headers and files into both archives, which
# `scripts/check_artifact.py` grades by opening the built artifacts.
license = "MIT"
license-files = ["LICENSE", "THIRD-PARTY-LICENSES.html"]
# No `Programming Language :: Python :: 3.x` rows. The wheel is `py3-none-<platform>`: it carries a
# native executable and no Python code at all, so it neither knows nor cares which interpreter
# installs it. Listing versions would advertise a compatibility matrix nothing here tests.
# `requires-python` below is the only floor a consumer sees: "the lowest interpreter we smoke".
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Operating System :: MacOS",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Rust",
    "Topic :: Software Development :: Quality Assurance",
]
# The distribution floor: the lowest interpreter `build-artifacts.yml` install-smokes, graded on the
# built archive by `scripts/check_artifact.py`. NOT the corpus-tooling floor — `corpus/measure.py`
# keeps its own, higher `MIN_INTERPRETER = (3, 12)`: pre-3.12 `tokenize` hides identifiers inside
# f-strings (PEP 701), lowering the restatement counts from 137 to 133 on an audited repository.
# `tests/unit/test_measure.py` pins this string and requires MIN_INTERPRETER to stay above it.
requires-python = ">=3.11"

# THERE IS DELIBERATELY NO `[project.scripts]` HERE, and its absence is load-bearing.
#
# With `bindings = "bin"` below, maturin puts the compiled executable into
# `tooprolix-<version>.data/scripts/tooprolix` and the installer copies it onto PATH itself — a
# console script would be a second, conflicting `tooprolix` in the same place. ruff has no
# `[project.scripts]` either, for the same reason.
#
# What checks it: `scripts/install-smoke.sh` requires `.venv/bin/tooprolix` to EXIST after the
# install, so "the command is on PATH" is asserted against the filesystem of a real install rather
# than inferred from this file.

[project.urls]
Repository = "https://github.com/golyshevskii/tooprolix"
Changelog = "https://github.com/golyshevskii/tooprolix/blob/main/CHANGELOG.md"
Documentation = "https://github.com/golyshevskii/tooprolix/blob/main/docs/cli-contract.md"
Issues = "https://github.com/golyshevskii/tooprolix/issues"

[tool.tooprolix]
# The tool run on itself. Until this key existed `tooprolix check .` on this repository was exit 2
# with zero findings, because `tests/fixtures/broken/syntax_error.py` is deliberately unparsable and
# deliberately committed — `.gitignore` cannot express "committed, but not part of the measurement",
# and an opt-out marker cannot save a file that never parses far enough for its comments to be read.
#
# The whole fixture tree is excluded and not just the one broken file, because the rest of it is
# equally not this repository's prose: `dup-corpus/` is duplicated on purpose so TPX003 has
# something to find, and measuring it would report our own test data as a defect in our own source.
#
# This prunes a directory during a WALK. `tooprolix check tests/fixtures/broken` still checks that
# tree and still exits 2 — the walker never sees `tests/fixtures` as an entry to prune when the walk
# starts below it, and a path named directly on the command line is checked regardless. That is
# ruff's default and it is what keeps the fixture tests honest.
exclude = ["tests/fixtures"]

[tool.maturin]
# The wheel carries the NATIVE EXECUTABLE, not an importable module.
#
# It is ruff's line, but the argument is this repository's own measurement: every corpus number was
# taken on the standalone binary `target/release/tooprolix` (`corpus/run_all.sh:53`,
# `corpus/determinism_check.sh:38`, `corpus/bench.py:55`). Shipping the extension module instead
# would publish the single class of artifact on which nothing here has ever been measured.
#
# What it produces, and what the smoke checks: `tooprolix-<version>.data/scripts/tooprolix` inside
# a `py3-none-<platform>` wheel — no `.so`, no Python source, no `__init__.py`. `import tooprolix`
# raises `ModuleNotFoundError`, on purpose.
bindings = "bin"
# NO `python-source` and therefore no shim. ruff ships `python/ruff/{__init__,__main__,
# _find_ruff}.py` alongside the binary; we ship the binary alone. The cost is named rather than
# hidden: `python -m tooprolix` does not work and integrators get no `find_tooprolix_bin`. Revisit
# when the pre-commit integration in the epic backlog lands — that is the consumer that would need
# it.
#
# `strip = true` is deliberately NOT here: stripping is decided in `[profile.release]` in
# Cargo.toml, where it was measured, so the standalone binary and the wheel's binary are built from
# ONE profile rather than two settings that happen to agree.
#
# That is a claim about the profile, not about bytes. Measured 2026-07-31 at `72fda14` on
# macOS/arm64: `maturin build --release` copies the cargo artifact unchanged (wheel executable and
# `target/release/tooprolix` both sha256 `b771fe92…`), but the bytes move with the environment —
# `SOURCE_DATE_EPOCH=1000000000` gives `004e52d1…` from identical source, because `build.rs` embeds
# the date. CI builds each published wheel per platform with its own `SOURCE_DATE_EPOCH`, and
# nothing compares a corpus binary's hash against wheel contents.
#
# # Third-party licences: generated from the locked dependency graph, not inferred from the SBOM
#
# The wheel's CycloneDX SBOM identifies components and licence expressions; it does not carry the
# licence/copyright texts those expressions may require on redistribution. `THIRD-PARTY-LICENSES.html`
# is generated from the published crate sources by cargo-about 0.9.1. The artifact workflow runs
# the same frozen generation and refuses any diff before building an sdist or wheel.
#
# `about.toml` chooses MIT first for `MIT OR Apache-2.0` dependencies and BSL-1.0 for ryu 1.0.23.
# Conjunction is not treated as choice: unicode-ident 1.0.24 keeps Unicode-3.0 and unicode_names2
# 1.3.0 keeps Unicode-DFS-2016. is-macro 0.3.7 is Apache-2.0-only, so its published Apache text is
# bundled; its crate archive has no NOTICE file. No conclusion depends on whether proc-macro code is
# present in the final binary — the text travels either way.
#
# cargo-about lists zerocopy 0.8.55 rather than separately naming zerocopy-derive 0.8.55. Both
# published archives contain byte-identical MIT/Apache/BSD licence files, so the generated text
# covers both components named separately by maturin's SBOM. This is licence-text coverage, not a
# claim of legal completeness. The project's own `License-Expression` remains MIT; the bundle is
# declared separately through `License-File` metadata and graded inside both distribution archives.

[dependency-groups]
lint = [
    "ruff>=0.16.0",
]
test = [
    "pytest>=9.1.1",
    # `make py.cov` only. It rides in the `test` group because that is the group `make test`
    # already installs and pytest-cov is inert unless `--cov` is passed — a second group would
    # mean a second resolve of the same pytest for no behavioural difference. It is pure Python,
    # so the three `--only-group` gates stay Rust-free (epic-1 rule, this task's AC4).
    "pytest-cov>=7.0.0",
    # `scripts/transform_readme.py` verifies its own output by RENDERING it — this is the library
    # PyPI renders descriptions with, and the one `twine check` already installs. It rides in the
    # `test` group because `tests/unit/test_transform_readme.py` is what exercises the verifier;
    # the build workflow supplies it with `uv run --with` instead, so it is not a runtime
    # dependency of the distribution.
    "readme-renderer[md]>=45.0",
]
type = [
    "ty>=0.0.63",
]

[tool.ruff]
line-length = 120
# target-version: inferred from requires-python, i.e. py311 — measured, not assumed
# (`ruff check --show-settings`). `build-artifacts.yml` runs `scripts/*.py` under 3.11, so a
# higher target here would greenlight a construct that syntax-errors during the release build.
# exclude: ruff's default list is a superset of what we'd write — keep the default.
# Fixture trees are test DATA and can never be lint-clean by construction:
#   - `corpus/fixtures/` holds the hand-counted oracle for tests/unit/test_measure.py. Its
#     exact physical line layout IS the expected value (19 lines, docstrings of 4/1/3 lines).
#     `ruff format` rewrote it once and turned three hand-counted assertions red — the file
#     must stay byte-stable or the test measures the formatter instead of the counter.
#   - `tests/fixtures/` is claimed by later tasks and will hold deliberately broken input
#     (task 6 AC2 needs a Python syntax error; task 3 needs prose-garbage files).
# LINT_PATHS/TY_PATHS already keep `make lint.check`/`make type` off these; this key is for
# whoever types a bare `ruff check .` and would otherwise face a red wall of correct files.
extend-exclude = ["corpus/fixtures", "tests/fixtures"]

[tool.ruff.format]
line-ending = "native"           # Line endings will be converted to \n on Unix and \r\n on Windows.
skip-magic-trailing-comma = true

[tool.ruff.lint]
extend-select = [
    "E",   # pycodestyle rules
    "ISC", # Checks for implicit literal string concatenation (auto-fixable)
    "PT",  # flake8-pytest-style rules
    "TC",  # Rules around TYPE_CHECKING blocks
    "UP",  # Pyupgrade
    "W",   # pycodestyle rules
    # By rule enables
    "B004",   # Checks for use of hasattr(x, "__call__") and replaces it with callable(x)
    "B006",   # Checks for uses of mutable objects as function argument defaults.
    "B007",   # Checks for unused variables in the loop
    "D101",   # Missing docstring in public class
    "D2",     # All D2* docstring rules, except ignored
    "D213",   # Conflicts with D212. Both can not be enabled. Multi-line docstring summary should start at the second line
    "D3",     # All D3* docstring rules, except ignored
    "D400",   # First line should end with a period in docstring
    "D401",   # First line of docstring should be in imperative mood. Example: Create dataset, not creates dataset
    "D402",   # First line should not be the function's signature
    "D403",   # First word of the docstring should be capitalized
    "D412",   # No blank lines allowed between a section header and its content
    "D419",   # Missing blank line after last section
    "I001",   # Organize imports
    "I002",   # Missing required import (auto-fixable)
    "PGH004", # Use specific rule codes when using noqa
    "PGH005", # Invalid unittest.mock.Mock methods/attributes/properties
    "RUF015", # Checks for unnecessary iterable allocation for first element
    "RUF019", # Checks for unnecessary key check
    "RUF100", # Unused noqa (auto-fixable)
    "TID25",  # flake8-tidy-imports rules
    "SIM300", # Checks for conditions that position a constant on the left-hand side of the comparison
              # operator, rather than the right-hand side.
]
# Dropped from the reference config, with reasons: ASYNC/LOG/G (no async and no logging-format
# surface in a synchronous measurement script) and RUF006 (asyncio-specific). PT* is KEPT —
# tests/unit/ is pytest.
ignore = [
    "D100", # Docstring at the top of every file.
    "D102", # Missing docstring in public method
    "D103", # Missing docstring in public function
    "D104", # Docstring at the top of every `__init__.py` file.
    "D105", # See https://lists.apache.org/thread/8jbg1dd2lr2cfydtqbjxsd6pb6q2wkc3
    "D107", # Docstring in every constructor is unnecessary if the class has a docstring.
    "D203", # 1 blank line required before class docstring
    "D205", # 1 blank line required between summary line and description
    "D212", # Conflicts with D213.  Both can not be enabled.
    "E731", # Do not assign a lambda expression, use a def
    "TC003", # Do not move imports from stdlib to TYPE_CHECKING block
    "PT006", # Wrong type of names in @pytest.mark.parametrize
    "PT007", # Wrong type of values in @pytest.mark.parametrize
    "PT011", # pytest.raises() is too broad, set the match parameter
    "PT013", # silly rule prohibiting e.g. `from pytest import param`
    "PT019", # fixture without value is injected as parameter, use @pytest.mark.usefixtures instead
    # Rules below explicitly set off which could overlap with Ruff's formatter
    # as it recommended by https://docs.astral.sh/ruff/formatter/#conflicting-lint-rules
    # Except ISC rules
    "W191",
    "E111",
    "E114",
    "E117",
    "D206",
    "D300",
    "Q000",
    "Q001",
    "Q002",
    "Q003",
    "COM812",
    "COM819",
    "E501", # Formatted code may exceed the line length, leading to line-too-long (E501) errors.
]

unfixable = [
    # PT022 replaces an empty `yield` with an empty `return`; it cannot reason about the
    # annotations that protect the fixture, so the "fix" is not always sound.
    "PT022",
]

[tool.ruff.lint.isort]
combine-as-imports = true
split-on-trailing-comma = false # If a comma is placed after the last member in a multi-line import,
                                # then the imports will never be folded into one line.

[tool.ty.environment]
# python-version: inferred from requires-python, same as ruff above (measured with a PEP 695 probe).
# Same reason as pytest's `pythonpath`: `measure.py` is a loose script in corpus/, not an
# installed package, so tests/unit/ imports it as a top-level module. ty needs the same search
# path or it cannot resolve the import. (ty rejects unknown config keys, so a typo here fails
# the gate loudly rather than being ignored.)
extra-paths = ["corpus", "scripts"]

[tool.ty.src]
# ty's equivalent of ruff's extend-exclude (verified: with this key `ty check .` passes on a
# tree whose only diagnostic is inside the excluded dir; without it, 1 diagnostic). Same
# reason as [tool.ruff] extend-exclude above — fixtures are data, not code.
exclude = ["corpus/fixtures", "tests/fixtures"]

[tool.ty.terminal]
error-on-warning = true # Exit with code 1 even if all diagnostics are warnings (strict CI)

[tool.ty.rules]
possibly-unresolved-reference = "error" # Treat unresolved references as errors, not warnings
unused-ignore-comment = "warn"          # Warn on dead `ty: ignore` comments

[tool.pytest.ini_options]
minversion = "9.0"
testpaths = ["tests/unit"]
# `measure.py` is a standalone script in corpus/ and is NOT part of the distributed package: the
# maturin backend above builds the Rust extension, not this throwaway research code, so `measure.py`
# is not importable via the project install no matter how the project is installed. `pythonpath` puts
# corpus/ on sys.path for the test session, which needs neither an `__init__.py` in a directory of
# throwaway code nor an editable install of the crate — and an editable install here would mean
# compiling Rust on every `make test`, which is exactly what `--only-group` avoids.
# Still current: this is the live mechanism, not a leftover from before the backend existed.
pythonpath = ["corpus", "scripts"]
addopts = [
    "-ra",              # Show short summary for all non-passing tests
    "--strict-config",  # Any warnings while parsing the pytest configuration raise errors
    "--strict-markers", # Markers not registered in configuration raise errors
    "--import-mode=importlib"
]

# Coverage of the PRODUCTION Python in this repository: the corpus tooling and the release scripts.
#
# `scripts/` was added on 2026-08-01 and the reason is worth keeping. It is the release-critical
# Python — `check_artifact.py` grades the built wheel and sdist, `transform_readme.py` rewrites the
# README that goes to PyPI — and it was the one directory whose failure ships a broken artifact and
# the one directory with no coverage signal, while `corpus/` (throwaway research tooling, see the
# Makefile's coverage block) was the whole denominator. Measured 2026-08-01: the three scripts are
# 88% / 84% / 87% covered in-process by `tests/unit/`, and the total moved 61.4% -> 65.7%.
# A script executed as a SUBPROCESS is still not attributable — that finding from the 2026-07-29
# pytest audit stands and is a different case; these three also have in-process unit tests.
#
# `source` is the DENOMINATOR, and that is why `tests/unit` is not in it even though the lint and
# type gates run on both (`LINT_PATHS`/`TY_PATHS` in the Makefile). A denominator that includes the
# test files makes the badge climb whenever someone writes more test code, which measures the
# opposite of what a coverage badge is for. `tests/unit` stays the runner's INPUT (`testpaths`
# above), never its subject.
#
# The second, independent statement of this list is `_SOURCE_TREES` in `scripts/coverage_report.py`,
# which walks the disk and fails the run when the two disagree.
[tool.coverage.run]
branch = true
source = ["corpus", "scripts"]
# coverage.py otherwise drops a `.coverage` SQLite file in the repository root, one `git add .` away
# from being committed. `target/coverage/` is where the JSON reports already go and `.gitignore`
# already covers `target`, so one line here beats a new ignore rule. Verified: with this key the
# root stays clean; without it `git status` shows `?? .coverage` after `make py.cov`.
data_file = "target/coverage/.coverage"
# Both of these are test DATA, and the first one is also a determinism hazard:
#   - `corpus/checkouts/` is six whole third-party repositories, cloned from `corpus.lock` on
#     demand and gitignored. It is present on a machine that has run `make corpus.measure` and
#     absent on a fresh clone, so leaving it in `source` would make the percentage depend on what
#     happens to be on disk — the badge would differ between this laptop and CI by thousands of
#     unexecuted files.
#   - `corpus/fixtures/sample.py` is the hand-counted oracle for tests/unit/test_measure.py. It is
#     input to a test, not code that runs.
omit = [
    "corpus/checkouts/*",
    "corpus/fixtures/*",
]

[tool.coverage.report]
# Show which lines are missing: the number on the badge is the headline, this is what an audit acts
# on. Tasks 9-11 are the consumers.
show_missing = true
skip_covered = false
