def handle(event):
    return event


# tooprolix: noqa TPX001
# The cache is warmed at start because a cold read costs whole seconds.
# The cache is warmed at start because a cold read costs whole seconds.
# The cache is warmed at start because a cold read costs whole seconds.
# The cache is warmed at start because a cold read costs whole seconds.
# The cache is warmed at start because a cold read costs whole seconds.
# The cache is warmed at start because a cold read costs whole seconds.
# The cache is warmed at start because a cold read costs whole seconds.
# The cache is warmed at start because a cold read costs whole seconds.
# The cache is warmed at start because a cold read costs whole seconds.
# The cache is warmed at start because a cold read costs whole seconds.
# The cache is warmed at start because a cold read costs whole seconds.
# The cache is warmed at start because a cold read costs whole seconds.
# The cache is warmed at start because a cold read costs whole seconds.
# The cache is warmed at start because a cold read costs whole seconds.
def run():
    return None
