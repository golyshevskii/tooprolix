def handle(event):
    return event


# tooprolix: noqa TPX003
# Logging is sampled here because the volume overwhelmed our collector twice.
# Logging is sampled here because the volume overwhelmed our collector twice.
# Logging is sampled here because the volume overwhelmed our collector twice.
# Logging is sampled here because the volume overwhelmed our collector twice.
# Logging is sampled here because the volume overwhelmed our collector twice.
# Logging is sampled here because the volume overwhelmed our collector twice.
# Logging is sampled here because the volume overwhelmed our collector twice.
# Logging is sampled here because the volume overwhelmed our collector twice.
# Logging is sampled here because the volume overwhelmed our collector twice.
# Logging is sampled here because the volume overwhelmed our collector twice.
# Logging is sampled here because the volume overwhelmed our collector twice.
# Logging is sampled here because the volume overwhelmed our collector twice.
# Logging is sampled here because the volume overwhelmed our collector twice.
# Logging is sampled here because the volume overwhelmed our collector twice.
def run():
    return None
