def process(batch):
    # tooprolix: noqa TPX002
    """Overview.
    Clocks are read once per batch because two reads straddle a boundary.
    Clocks are read once per batch because two reads straddle a boundary.
    Clocks are read once per batch because two reads straddle a boundary.
    Clocks are read once per batch because two reads straddle a boundary.
    Clocks are read once per batch because two reads straddle a boundary.
    Clocks are read once per batch because two reads straddle a boundary.
    Clocks are read once per batch because two reads straddle a boundary.
    Clocks are read once per batch because two reads straddle a boundary.
    Clocks are read once per batch because two reads straddle a boundary.
    Clocks are read once per batch because two reads straddle a boundary.
    Clocks are read once per batch because two reads straddle a boundary.
    Clocks are read once per batch because two reads straddle a boundary.
    Clocks are read once per batch because two reads straddle a boundary.
    Clocks are read once per batch because two reads straddle a boundary.
    Clocks are read once per batch because two reads straddle a boundary.
    Clocks are read once per batch because two reads straddle a boundary.
    Clocks are read once per batch because two reads straddle a boundary.
    Clocks are read once per batch because two reads straddle a boundary.
    """
    return batch
