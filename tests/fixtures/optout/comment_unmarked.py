def handle(event):
    return event


# The retry budget is small because the upstream service rate limits us.
# The retry budget is small because the upstream service rate limits us.
# The retry budget is small because the upstream service rate limits us.
# The retry budget is small because the upstream service rate limits us.
# The retry budget is small because the upstream service rate limits us.
# The retry budget is small because the upstream service rate limits us.
# The retry budget is small because the upstream service rate limits us.
# The retry budget is small because the upstream service rate limits us.
# The retry budget is small because the upstream service rate limits us.
# The retry budget is small because the upstream service rate limits us.
# The retry budget is small because the upstream service rate limits us.
# The retry budget is small because the upstream service rate limits us.
# The retry budget is small because the upstream service rate limits us.
# The retry budget is small because the upstream service rate limits us.
def run():
    return None
