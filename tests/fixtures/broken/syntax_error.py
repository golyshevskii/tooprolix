# This file does not parse, and that is the whole point of the fixture.
# A Python syntax error must reach the user as a tool error rather than as
# a quiet report of "no prose findings here".
def broken(:
    return 1
